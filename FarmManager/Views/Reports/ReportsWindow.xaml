<Window x:Class="FarmManager.App.Views.Reports.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FarmManager.App.Views.Reports"
        xmlns:app="clr-namespace:FarmManager.App.Helpers"
        mc:Ignorable="d"
        Language="pl-PL"
        Title="Wybór sezonu do raportu" Height="720" Width="900"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding ReportsViewModel, Source={StaticResource Locator}}">
    <Grid>
        <DataGrid HorizontalAlignment="Stretch" Grid.Row="0" FontSize="20" VerticalAlignment="Top" x:Name="DataGridCardStack" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" CanUserAddRows="False" IsReadOnly="True" AutoGenerateColumns="False" ItemsSource="{Binding Seasons}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa" Width="*" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Początek" Width="*" Binding="{Binding StartDate}"/>
                <DataGridTextColumn Header="Koniec" Width="*" Binding="{Binding EndDate}"/>
            </DataGrid.Columns>
            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding Open}"/>
            </DataGrid.InputBindings>
        </DataGrid>
    </Grid>
</Window>
